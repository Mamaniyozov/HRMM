[tool.black]
line-length = 120
target-version = ["py310"]
include = '\.pyi?$'
extend-exclude = '''
/(
    \.git
  | \.venv
  | venv
  | migrations
  | staticfiles
  | node_modules
)/
'''

[tool.isort]
profile = "black"
line_length = 120
skip = ["migrations"]
